<% if notice %><p id="notice" class="alert alert-success"><%= notice %></p><% end %>

<p>
  <strong>Name:</strong>
  <%= @criterium.name %>
</p>

<%= link_to 'Edit', edit_criterium_path(@criterium), class: 'btn btn-primary' %> |
<%= link_to 'Back', criteria_path, class: 'btn btn-primary' %>
