<p id="notice"><%= notice %></p>

<p>
  <strong>Kriteria:</strong>
  <%= @subcriterium.criterium.name %>
</p>
<p>
  <strong>Nama Sub Kriteria:</strong>
  <%= @subcriterium.name %>
</p>

<%= link_to 'Edit', edit_criterium_subcriterium_path(@subcriterium, id: @subcriterium.id), class: 'btn btn-block btn-primary' %>
<%= link_to 'Kembali', criterium_subcriteria_path(@subcriterium), class: 'btn btn-block btn-default' %>
