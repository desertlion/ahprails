<% if notice %>
<p id="notice" class="alert alert-info"><%= notice %></p>
<% end %>

<h1>Daftar Tanaman</h1>

<table class="table table-bordered table-condensed table-striped">
  <thead>
    <tr>
      <th>Nama Tanaman</th>
      <th>Aksi</th>
    </tr>
  </thead>

  <tbody>
    <% @plants.each do |plant| %>
      <tr>
        <td><%= plant.name %></td>
        <td>
          <div class="btn-group">
            <%= link_to 'Lihat', plant, class: 'btn btn-mini btn-default' %>
            <%= link_to 'Edit', edit_plant_path(plant), class: 'btn btn-mini btn-default' %>
            <%= link_to 'Hapus', plant, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-mini btn-danger' %>
          </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Input Tanaman Baru', new_plant_path, class: 'btn btn-mini btn-primary btn-block btn-raised' %>
